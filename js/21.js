/** Scope */

const precio = 300;

function unaFuncion() {
    const precio = 600;
    console.log(precio);
}

console.log(precio);

unaFuncion();

/** Las funciones puedes ser redeclaradas unicamente
 *  dentro de una funcion o un if, dentro de las llaves
 */

